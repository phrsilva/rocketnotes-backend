const userCreateService = require('./UserCreateService');

it("Usuário deve ser criado", () => {
    const user = {
        name: "Rauner",
        email: "rauner@raunerlimpezas.com.br",
        password: "123456"
    }
})